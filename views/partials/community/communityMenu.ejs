<div id="sidebar-wrapper" style="margin-top:60px">
  <ul class="sidebar-nav nav-pills nav-stacked" id="menu">

    <li class="mobile-view-dp">
      <div style="margin-right: 10px">
        <center>
          <img src="../../../upload/Profile/<%= user.ProfilePic %>" style="height: 60px;width: 60px;border-radius:50%">
        </center>
      </div>
    </li>

    <li class="onlyForPcNav">
      <a onclick="openSideNav()">
        <span class="fa-stack fa-lg pull-left">
          <i class="fa fa-bars fa-stack-1x "></i>
        </span>cq</a>
    </li>


    <li id="edit-profile-nav">
      <a href="/profile"><span class="fa-stack fa-lg pull-left">
          <i class="fa fa-user fa-stack-1x "></i></span><%= user.Name %></a>
    </li>


    <li id="community-panel-nav">
      <a href="/community/communitypanel"><span class="fa-stack fa-lg pull-left">
          <i class="fa fa-users fa-stack-1x "></i></span>Communities</a>
    </li>

    <% if(user.Role.toLowerCase() == 'admin' || user.Role.toLowerCase() == 'superuser'){ %>
      <li id="switch-asuser-nav">
        <a onclick="switchState('Switch As Admin')"><span class="fa-stack fa-lg pull-left">
            <i class="fa fa-toggle-on fa-stack-1x "></i></span>Switch As Admin</a>
      </li>
    <% } %>

    <li id="change-password-nav">
      <a href="/changePassword"><span class="fa-stack fa-lg pull-left">
          <i class="fa fa-lock fa-stack-1x "></i></span>Change Password</a>
    </li>

    <li>
      <a onclick="logout()"><span class="fa-stack fa-lg pull-left">
          <i class="fa fa-sign-out-alt fa-stack-1x "></i></span>Logout</a>
    </li>

  </ul>
</div>

<script type="text/javascript">
  $("#menu-toggle").click(function (e) {
    e.preventDefault();
    $("#wrapper").toggleClass("toggled");
  });

  $("#menu-toggle-pc").click(function (e) {
    e.preventDefault();
    $("#wrapper").toggleClass("toggled-pc");
    $('#menu ul').hide();
  });

  //----------------------On Hoover or onclick menuBar-------------------------------
  function openSideNav() {
    $("#wrapper").toggleClass("toggled-pc");
    $('#menu ul').hide();
  }
  // $("#sidebar-wrapper").hover(function(){
  //   $("#wrapper").toggleClass("toggled-pc");
  //   $('#menu ul').hide();
  // });
  //----------------------On Hoover or onclick menuBar-------------------------------

  function initMenu() {
    // $("#wrapper").toggleClass("toggled-pc");
    // $('#menu ul').hide();

    $('#menu ul').hide();
    $('#menu ul').children('.current').parent().show();
    $('#menu li a').click(
      function () {
        var checkElement = $(this).next();
        if ((checkElement.is('ul')) && (checkElement.is(':visible'))) {
          return false;
        }
        if ((checkElement.is('ul')) && (!checkElement.is(':visible'))) {
          $('#menu ul:visible').slideUp('normal');
          checkElement.slideDown('normal');
          return false;
        }
      }
    );
  }
  $(document).ready(function () {
    initMenu();
  });
</script>

<script>

  function logout() {
    location.assign('/logout');
  }

</script>
<style>
  .community-name {
    border: 0;
    font-family: 'Open Sans', sans-serif;
    font-weight: bold;
    word-wrap: break-word;
    font-size: 17px;
    padding: 12px 0 0 0;
    outline: none !important;
    background-color: rgba(0, 0, 0, 0);
  }

  .community-description {
    border: 0;
    font-family: 'Open Sans', sans-serif;
    word-wrap: break-word;
    font-size: 16px;
    color: #000;
    outline: none !important;
    background-color: rgba(0, 0, 0, 0);
  }

  .totalUsers {
    padding: 0;
    margin: 0;
    font-weight: 600;
    color: #7D7D7D;
    text-decoration: none;
    word-wrap: break-word;
    text-transform: capitalize;
  }

  .community-description-full {
    display: none;
  }

  @media(max-width:768px) {
    .community-name {
      border: 0;
      font-family: 'Open Sans', sans-serif;
      font-weight: bold;
      padding: 15px 0 0 5px;
      overflow: scroll;
      word-wrap: break-word;
      font-size: 15px;
      color: #000;
      outline: none !important;
      background-color: rgba(0, 0, 0, 0);
    }
  }
</style>
<script>
  var selector = '.nav li';
  $(selector).removeClass('active');
  $('#community-panel-nav').addClass('active');
</script>
<script src="/javascripts/Common.js"></script>